#include <iostream>
#include <vector>
using namespace std;

vector<int> solution(int n, int s) {
    vector<int> answer;
    vector<int> numbers(n);

    int max_number = 0;

    for (int i = 0; i < n; i++) {

    }

    return answer;
}

int main() {
    std::cout << solution(2, 8)[0];
}
